<!-- Shared styles — commun à tous les layouts TRMNL Portaki -->
<style>
  .portaki-logo {
    font-size: 9px;
    color: #999;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .stay-pill {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 600;
  }
  .pill--active { background: #000; color: #fff; }
  .pill--upcoming { background: #eee; color: #333; }
  .pill--completed { background: #f5f5f5; color: #999; }

  .progress-bar {
    height: 4px;
    background: #eee;
    border-radius: 2px;
    overflow: hidden;
  }
  .progress-bar__fill {
    height: 100%;
    background: #000;
    border-radius: 2px;
  }
</style>
