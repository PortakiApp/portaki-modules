name: CI

on:
  push:
    branches:
      - develop
      - main
      - master
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read

jobs:
  workspace:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://npm.pkg.github.com"

      - name: Install dependencies
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: pnpm install

      - name: Lint (workspace)
        run: pnpm lint

  pre-arrival-form-backend:
    name: Maven verify (pre-arrival-form backend)
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout portaki-sdk (Java SDK — GPR artefact often absent until published)
        uses: actions/checkout@v4
        with:
          repository: PortakiApp/portaki-sdk
          path: .deps/portaki-sdk
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Maven settings.xml (GitHub Packages)
        env:
          GPR_ACTOR: ${{ github.actor }}
          GPR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash .github/scripts/configure-maven-gpr.sh

      - name: Install portaki-module-sdk into local Maven repo
        run: bash .github/scripts/ci-install-portaki-java-sdk.sh

      - name: Verify backend (Maven)
        working-directory: modules/pre-arrival-form/backend
        run: mvn -B verify
